import { Telegraf, Context } from 'telegraf';
/**
 * Setup all bot commands and handlers
 * Clean architecture with separation of concerns
 */
export declare function setupCommands(bot: Telegraf<Context>): void;
